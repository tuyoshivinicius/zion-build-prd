# R8 — Endereçar o dia 2 (`/zion-prd-evolve`) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Dar ao harness um dia 2 com dono — a 10ª skill `/zion-prd-evolve`, ponto de entrada único para mudanças pós-release — apoiada por changelog na PRD (§13), supersessão de ADR, re-decomposição parcial, checks mecânicos novos e fixtures.

**Architecture:** Uma skill roteadora nova (`/zion-prd-evolve`) que classifica a mudança em cenários canônicos (C1/C2/C3), executa inline só o barato e local (changelog §13 + edição pontual §6/§8) e **para em cada gate** delegando o resto às skills existentes, que ganham modos mínimos (`--substitui` no adr-new, `--epico` no decompose, re-specify no specify-prompt). Duas regras decidíveis novas viram checks nos scripts (`check-prd.sh` sobre §13/§8; `check-adr.sh` sobre a simetria da supersessão), com fixtures nas duas camadas de avaliação (mecânica + LLM). Todos os gates aconselham, nunca bloqueiam.

**Tech Stack:** Skills do Claude Code (Markdown + frontmatter), scripts Bash/awk (POSIX-portável), fixtures Markdown, distribuição por `asset-map.sh` + `sync-assets.sh`. Sem código de aplicação.

---

## File Structure

**Assets canônicos (fonte única — propagados para `references/` pelo sync):**
- `assets/quality-rules.md` — ganha a âncora `#dia-2` (cenários C1/C2/C3, regras do changelog §13, simetria da supersessão). Sincronizado para muitas skills.
- `assets/templates/prd-skeleton.md` — ganha a **§13 "Histórico de mudanças"** (vazia no dia 1). Sincronizado para `zion-prd-write`.

**Scripts (verificação mecânica):**
- `scripts/check-prd.sh` — checks novos sobre §13 (changelog) e §8 (restrição morta). Sincronizado para `zion-prd-write`, `zion-prd-specify-prompt` e (após Task 5) `zion-prd-evolve`.
- `scripts/check-adr.sh` — check novo de **simetria** da supersessão. Sincronizado para `zion-prd-spike` e (após Task 5) `zion-prd-evolve`.
- `scripts/test-check-prd.sh`, `scripts/test-check-adr.sh` — ganham asserts novos (rodam por `scripts/eval.sh`, que o CI já executa).
- `scripts/asset-map.sh` — registra a skill nova e suas references.

**Skill nova:**
- `skills/zion-prd-evolve/SKILL.md` — a 10ª skill (references sincronizadas em Task 5).

**Skills existentes (modos mínimos, edição de SKILL.md só):**
- `skills/zion-adr-new/SKILL.md` — modo substituir.
- `skills/zion-prd-decompose/SKILL.md` — modo parcial (`--epico`).
- `skills/zion-prd-specify-prompt/SKILL.md` — enquadramento re-specify.

**Fixtures:**
- `scripts/fixtures/prd-evolve/{clean,dirty}/PRD.md` + `.../adr/ADR-*.md` — camada mecânica do check-prd (§13/§8).
- `scripts/fixtures/adr/{superseded-clean,superseded-dirty}/ADR-*.md` — camada mecânica do check-adr (simetria).
- `scripts/fixtures/skills/evolve/{parece-c2-mas-c3,limpa}/{mudanca.md,esperado.md}` — camada LLM (classificação da Fase 1).

**Documentação:**
- `docs/guia-prd-para-spec-kit.md` — novo "Passo 7 — O dia 2".
- `docs/como-usar.md` — seção do evolve, mapa rápido, nó no mermaid, contagens (9→10), drift "7 comandos" (H7), "12 seções"→"13 seções".
- `docs/avaliacao-harness.md` — índice das fixtures LLM do evolve.
- `README.md` — lista de skills (+evolve).

---

## Task 1: Assets — `#dia-2` no quality-rules + §13 no skeleton

**Files:**
- Modify: `assets/quality-rules.md` (append após o bloco `#denylist`)
- Modify: `assets/templates/prd-skeleton.md` (append §13 após §12)
- Test: `scripts/test-check-prd.sh` (deve seguir verde — §13 vazia não dispara nada)

- [ ] **Step 1: Adicionar a âncora `#dia-2` ao final do `assets/quality-rules.md`**

Localize o fim do bloco denylist (as três últimas linhas do arquivo):

```
three.js
typescript
```
```

Substitua **essa sequência exata** (os dois termos finais + a cerca de fechamento) por ela mesma seguida da nova seção:

````text
three.js
typescript
```

## Dia 2 — evolução pós-release {#dia-2}

Depois da release 1, requisitos mudam. O ponto de entrada único é `/zion-prd-evolve`, que versiona a PRD
e roteia para os comandos donos de cada artefato, parando em cada gate. Uma mudança se classifica em um
ou mais **cenários canônicos** (pode combinar mais de um):

- **C1 — RF novo:** requisito que não existia. Toca a §6 (RF no épico certo ou num épico novo), o
  changelog (§13), o re-fatiamento **parcial** do épico e a tabela (§12 via trace).
- **C2 — RF alterado ou removido:** requisito muda de significado ou sai de escopo. Toca a §6, o §13, as
  fatias do épico afetado e a tabela; fatia já com `spec.md` → prompt de **re-specify** pela ponte.
- **C3 — Decisão revertida:** decisão estruturante caiu. Nasce um ADR que **substitui** o antigo
  (referência simétrica) + §8 (restrições) + §13 + aviso de revisar a `constitution`.

> Cuidado com a decisão que cai **disfarçada de requisito**: se uma mudança que parece só um RF alterado
> (C2) só se resolve trocando uma decisão já registrada num ADR, é **C3**.

### Changelog da PRD (§13) — regras decidíveis

A §13 "Histórico de mudanças" é uma tabela, **uma linha por mudança**, escrita por `/zion-prd-evolve`
(edição manual continua possível):

| Data | Cenário | Mudança | Motivo | Artefatos afetados |
|------|---------|---------|--------|--------------------|
| 2026-08-02 | C2 | `RF-07` alterado: exportar SVG em vez de PNG | feedback de usuários | ADR-002 → ADR-005 · fatia S4 re-especificada |

Regras que o `check-prd.sh` verifica sobre a §13 (a §13 é **opcional** — PRD sem ela, dia 1 ou pré-R8,
não dispara estes checks):

- Todo `RF-xx` citado no changelog **existe na §6** — ou a própria linha o declara **"removido"**.
- Todo `ADR-xxx` citado **existe em `docs/adr/`**.
- A coluna **Cenário** usa só **C1**, **C2** ou **C3**.
- Check cruzado sobre a §8: uma restrição apontando um ADR com `Status: Substituído por` é uma
  **restrição morta** → o script acusa.

### Supersessão de ADR — referência simétrica

Quando uma decisão cai (C3), `/zion-adr-new "<título>" --substitui ADR-<n>` cria o ADR novo e edita o
antigo, deixando uma referência **cruzada e simétrica**:

- No ADR novo (ADR-`<m>`): campo de cabeçalho `- **Substitui:** ADR-<n>`.
- No ADR antigo (ADR-`<n>`): cabeçalho `- **Status:** Substituído por ADR-<m>`.

O `check-adr.sh` verifica a **simetria**: `Status: Substituído por ADR-<m>` exige que ADR-`<m>` exista e
declare `Substitui: ADR-<n>`, e vice-versa. Referência quebrada ou unilateral → o script acusa.
````

- [ ] **Step 2: Adicionar a §13 ao `assets/templates/prd-skeleton.md`**

Localize a §12 (últimas duas linhas do arquivo):

```
## 12. Rastreabilidade
Tabela de rastreabilidade RF → épico → fatia, injetada por `/zion-prd-decompose` e mantida dentro desta PRD.
```

Substitua por ela mesma seguida da §13 (só cabeçalho + separador, **sem linhas de dados** — para não
disparar os checks numa PRD recém-escrita):

```markdown
## 12. Rastreabilidade
Tabela de rastreabilidade RF → épico → fatia, injetada por `/zion-prd-decompose` e mantida dentro desta PRD.

## 13. Histórico de mudanças
> Vazia no dia 1. Preenchida por `/zion-prd-evolve` a partir da primeira mudança pós-release — uma linha
> por mudança. Regras do formato em `quality-rules.md` `#dia-2`; edição manual continua possível.

| Data | Cenário | Mudança | Motivo | Artefatos afetados |
|------|---------|---------|--------|--------------------|
```

- [ ] **Step 3: Sincronizar as references derivadas**

Run: `./scripts/sync-assets.sh`
Expected: `sync-assets: ok`

- [ ] **Step 4: Confirmar que não há drift**

Run: `./scripts/check-assets.sh`
Expected: `check-assets: sem drift`

- [ ] **Step 5: Confirmar que os self-tests seguem verdes (§13 vazia não dispara nada)**

Run: `./scripts/eval.sh`
Expected: termina com `eval: tudo verde` (o check-prd ainda não conhece os checks da §13, e a §13 do
skeleton está vazia — nada muda).

- [ ] **Step 6: Commit**

```bash
git add assets/quality-rules.md assets/templates/prd-skeleton.md skills/*/references/quality-rules.md skills/zion-prd-write/references/prd-skeleton.md
git commit -m "feat(assets): âncora #dia-2 no quality-rules e §13 no esqueleto da PRD (R8)"
```

---

## Task 2: `check-prd.sh` — checks da §13 (changelog) e §8 (restrição morta)

**Files:**
- Create: `scripts/fixtures/prd-evolve/clean/PRD.md`
- Create: `scripts/fixtures/prd-evolve/clean/adr/ADR-002-motor-raster.md`
- Create: `scripts/fixtures/prd-evolve/clean/adr/ADR-005-motor-vetor.md`
- Create: `scripts/fixtures/prd-evolve/dirty/PRD.md`
- Create: `scripts/fixtures/prd-evolve/dirty/adr/ADR-002-motor-raster.md`
- Create: `scripts/fixtures/prd-evolve/dirty/adr/ADR-005-motor-vetor.md`
- Modify: `scripts/check-prd.sh`
- Test: `scripts/test-check-prd.sh`

- [ ] **Step 1: Criar a fixture limpa `scripts/fixtures/prd-evolve/clean/PRD.md`**

Tudo coerente: §13 cita só RF/ADR existentes com Cenário C1/C2/C3; §8 aponta o ADR **novo** (não
substituído).

```markdown
# PRD — Painel de Tarefas

## 1. Visão
Para a gerente de projetos, que perde tempo consolidando status, o Painel reúne o andamento numa tela só.

## 2. Objetivos & métricas
- Reduzir o tempo de consolidação de 60 para 10 minutos por semana.

## 3. Personas
1. Gerente de projetos — acompanha o time.

## 4. Escopo (in / out)
- **Faz (in):** ver e atualizar o status das tarefas; exportar o quadro.
- **Não faz (out):** controle de horas e faturamento.

## 5. Regras de negócio (RN-xx)
- RN-01 Uma tarefa pertence a exatamente um responsável.

## 6. Requisitos funcionais por épico (RF-xx)
- **Épico E1 — Acompanhamento:** RF-01 a gerente vê o status agregado; RF-02 a gerente filtra por responsável.
- **Épico E2 — Atualização:** RF-03 o responsável marca uma tarefa como concluída; RF-04 o responsável exporta o quadro em vetor.

## 7. NFRs (com números)
- A tela carrega em até 2 segundos com 500 tarefas.
- Disponibilidade de 99,9% ao mês.

## 8. Restrições (das ADRs)
- Ver docs/adr/ADR-005 para a decisão de exportação.

## 9. Glossário
- Tarefa: unidade de trabalho atribuída a um responsável.

## 10. Riscos
- Adoção baixa se a atualização for trabalhosa; mitigar com fluxo de um clique.

## 11. Questões abertas
- [NEEDS CLARIFICATION] limite de tarefas por projeto.

## 12. Rastreabilidade
| RF | Épico | Fatia |
|----|-------|-------|
| RF-01 | E1 | R0 |

## 13. Histórico de mudanças
| Data | Cenário | Mudança | Motivo | Artefatos afetados |
|------|---------|---------|--------|--------------------|
| 2026-08-02 | C3 | exportação passa a ser vetor | motor antigo não gerava vetor | ADR-002 → ADR-005 · restrição §8 atualizada |
| 2026-08-05 | C1 | RF-04 novo: exportar o quadro em vetor | pedido recorrente | RF-04 no épico E2 · fatia nova |
```

- [ ] **Step 2: Criar os ADRs da fixture limpa**

`scripts/fixtures/prd-evolve/clean/adr/ADR-002-motor-raster.md`:

```markdown
# ADR-002 — Motor de exportação raster

- **Status:** Substituído por ADR-005
- **Data:** 2026-07-17
- **Decisores:** time
- **Evidência:** https://exemplo.org/comparativo-exportacao (licença MIT)

## Contexto
Risco de conhecimento. Decisão documentável sem rodar.

## Decisão
Exportar em raster.

## Consequências
Trade-offs aceitos.

## Status
Substituído por ADR-005.
```

`scripts/fixtures/prd-evolve/clean/adr/ADR-005-motor-vetor.md`:

```markdown
# ADR-005 — Motor de exportação vetorial

- **Status:** Aceito
- **Data:** 2026-08-02
- **Decisores:** time
- **Substitui:** ADR-002
- **Evidência:** https://exemplo.org/exportacao-vetor (licença MIT)

## Contexto
A decisão anterior (ADR-002) caiu: usuários precisam de vetor. Este ADR a substitui.

## Decisão
Exportar em vetor. Descartado o raster do ADR-002.

## Consequências
Trade-offs aceitos.

## Status
Aceito.
```

- [ ] **Step 3: Criar a fixture suja `scripts/fixtures/prd-evolve/dirty/PRD.md`**

Quatro defeitos alvo: §8 aponta o ADR **substituído** (ADR-002) → `restricao-morta`; §13 cita `RF-99`
inexistente (sem "removido") → `changelog-rf-inexistente`; cita `ADR-404` inexistente →
`changelog-adr-inexistente`; usa Cenário `X` → `changelog-cenario-invalido`.

```markdown
# PRD — Painel de Tarefas

## 1. Visão
Para a gerente de projetos, que perde tempo consolidando status, o Painel reúne o andamento numa tela só.

## 4. Escopo (in / out)
- **Faz (in):** ver e atualizar o status das tarefas.
- **Não faz (out):** controle de horas.

## 6. Requisitos funcionais por épico (RF-xx)
- **Épico E1 — Acompanhamento:** RF-01 a gerente vê o status agregado; RF-02 a gerente filtra por responsável.
- **Épico E2 — Atualização:** RF-03 o responsável marca uma tarefa como concluída.

## 7. NFRs (com números)
- A tela carrega em até 2 segundos com 500 tarefas.

## 8. Restrições (das ADRs)
- Ver docs/adr/ADR-002 para a decisão de exportação.

## 12. Rastreabilidade
| RF | Épico | Fatia |
|----|-------|-------|
| RF-01 | E1 | R0 |

## 13. Histórico de mudanças
| Data | Cenário | Mudança | Motivo | Artefatos afetados |
|------|---------|---------|--------|--------------------|
| 2026-08-02 | C2 | RF-99 alterado: exportar em vetor | feedback | ADR-404 novo |
| 2026-08-05 | X | RF-03 alterado | ajuste | fatia S2 |
```

- [ ] **Step 4: Criar os ADRs da fixture suja**

`scripts/fixtures/prd-evolve/dirty/adr/ADR-002-motor-raster.md` — idêntico ao da fixture limpa (Step 2), com `Status: Substituído por ADR-005`:

```markdown
# ADR-002 — Motor de exportação raster

- **Status:** Substituído por ADR-005
- **Data:** 2026-07-17
- **Decisores:** time
- **Evidência:** https://exemplo.org/comparativo-exportacao (licença MIT)

## Contexto
Risco de conhecimento. Decisão documentável sem rodar.

## Decisão
Exportar em raster.

## Consequências
Trade-offs aceitos.

## Status
Substituído por ADR-005.
```

`scripts/fixtures/prd-evolve/dirty/adr/ADR-005-motor-vetor.md` — idêntico ao da fixture limpa (Step 2):

```markdown
# ADR-005 — Motor de exportação vetorial

- **Status:** Aceito
- **Data:** 2026-08-02
- **Decisores:** time
- **Substitui:** ADR-002
- **Evidência:** https://exemplo.org/exportacao-vetor (licença MIT)

## Contexto
A decisão anterior (ADR-002) caiu: usuários precisam de vetor. Este ADR a substitui.

## Decisão
Exportar em vetor. Descartado o raster do ADR-002.

## Consequências
Trade-offs aceitos.

## Status
Aceito.
```

- [ ] **Step 5: Escrever os asserts falhando em `scripts/test-check-prd.sh`**

Localize o bloco final:

```bash
if [ "$fail" -eq 0 ]; then echo "test-check-prd: tudo verde"; else echo "test-check-prd: FALHOU"; exit 1; fi
```

Insira **antes** dele os novos casos 6 e 7:

```bash
# 6. PRD dia-2 limpa (§13 + §8 coerentes com docs/adr/ vizinho) → exit 0
out="$(bash "$CHECK" prd "$FIX/prd-evolve/clean/PRD.md")"; rc=$?
assert_exit "prd dia-2 limpa sai 0" 0 "$rc"
assert_contains "prd dia-2 limpa reporta limpo" "check-prd: limpo" "$out"

# 7. PRD dia-2 suja → exit 1 + um achado de cada tipo novo
out="$(bash "$CHECK" prd "$FIX/prd-evolve/dirty/PRD.md")"; rc=$?
assert_exit "prd dia-2 suja sai 1" 1 "$rc"
assert_contains "acha changelog-rf-inexistente"   "changelog-rf-inexistente"   "$out"
assert_contains "acha changelog-cenario-invalido" "changelog-cenario-invalido" "$out"
assert_contains "acha changelog-adr-inexistente"  "changelog-adr-inexistente"  "$out"
assert_contains "acha restricao-morta"            "restricao-morta"            "$out"

if [ "$fail" -eq 0 ]; then echo "test-check-prd: tudo verde"; else echo "test-check-prd: FALHOU"; exit 1; fi
```

(Apague a linha `if [ "$fail" -eq 0 ]...` antiga — ela agora vem depois dos novos casos.)

- [ ] **Step 6: Rodar o teste e verificar que FALHA (checks ainda não existem)**

Run: `bash scripts/test-check-prd.sh`
Expected: FALHA — `FALHOU: prd dia-2 suja sai 1` (a PRD suja ainda sai 0 porque os checks novos não
existem) e os `assert_contains` dos achados novos falham.

- [ ] **Step 7: Descobrir os ADRs vizinhos no `scripts/check-prd.sh`**

Localize:

```bash
  SRC="$target"; LABEL="$(basename "$target")"
fi

# --- checks (preenchidos nas próximas tasks) ---
```

Substitua por (insere o bloco de descoberta de ADRs entre o `fi` e o comentário):

```bash
  SRC="$target"; LABEL="$(basename "$target")"
fi

# --- R8: descobre os ADRs vizinhos da PRD (docs/adr/ ao lado do arquivo) para os checks §13/§8. ---
# Só para arquivo real (não stdin); degrada em silêncio se docs/adr/ não existir.
ADR_DIR="$(dirname "$SRC")/adr"
adr_ids=""; superseded_ids=""
if [ "$target" != "-" ] && [ -d "$ADR_DIR" ]; then
  for af in "$ADR_DIR"/ADR-*.md; do
    [ -f "$af" ] || continue
    aid="$(basename "$af" | grep -oE '^ADR-[0-9]+')"
    [ -n "$aid" ] || continue
    adr_ids="$adr_ids $aid"
    ast="$(sed -n 's/^[[:space:]]*-[[:space:]]*\*\*Status:\*\*[[:space:]]*//p' "$af" | head -1)"
    if printf '%s' "$ast" | grep -qiE 'Substitu[ií]do por[[:space:]]+ADR-[0-9]+'; then
      superseded_ids="$superseded_ids $aid"
    fi
  done
fi

# --- checks (preenchidos nas próximas tasks) ---
```

- [ ] **Step 8: Adicionar as funções `check_changelog` e `check_restricao_morta`**

Localize o fechamento do `check_rf` seguido do `case` (fim do arquivo, área dos checks):

```bash
  ' "$SRC"
}

case "$mode" in
  prd)     findings="$(check_stack; check_nfr; check_rf)" ;;
  specify) findings="$(check_stack; check_rf_cobertos)" ;;
esac
```

Substitua por (as duas funções novas + o `case` com os checks novos no modo `prd`):

```bash
  ' "$SRC"
}
# Seção 13 (changelog): cada linha de dados da tabela — Cenário ∈ {C1,C2,C3}; todo RF-xx citado existe
# na §6 (ou a linha diz "removido"); todo ADR-xxx citado existe em docs/adr/ (só quando a lista foi
# descoberta). §13 ausente → nada dispara (compatível com PRDs pré-R8 / dia 1).
check_changelog() {
  awk -v label="$LABEL" -v adrs="$adr_ids" '
    BEGIN { na=split(adrs,aa,/[[:space:]]+/); for(i=1;i<=na;i++) if(aa[i]!="") have_adr[aa[i]]=1 }
    /^## / { sec=$2; sub(/\./,"",sec); next }
    sec=="6" {
      t=$0; while (match(t,/RF-[0-9]+/)) { have_rf[substr(t,RSTART,RLENGTH)]=1; t=substr(t,RSTART+RLENGTH) }
      next
    }
    sec=="13" {
      if ($0 !~ /^[[:space:]]*\|/) next
      bar=$0; gsub(/[[:space:]]/,"",bar); if (bar ~ /^\|(:?-+:?\|)+$/) next
      split($0,col,/\|/); cen=col[3]; gsub(/^[[:space:]]+|[[:space:]]+$/,"",cen)
      if (cen=="Cenário" || col[2] ~ /^[[:space:]]*Data[[:space:]]*$/) next
      if (cen !~ /^C[123]$/)
        printf "%s:%d: changelog-cenario-invalido — \"%s\" (use só C1/C2/C3)\n", label, NR, cen
      removido = ($0 ~ /removido/)
      t=$0; while (match(t,/RF-[0-9]+/)) { rf=substr(t,RSTART,RLENGTH); t=substr(t,RSTART+RLENGTH)
        if (!(rf in have_rf) && !removido)
          printf "%s:%d: changelog-rf-inexistente — \"%s\" não existe na §6 (ou declare \"removido\")\n", label, NR, rf }
      if (adrs != "") { t=$0; while (match(t,/ADR-[0-9]+/)) { adr=substr(t,RSTART,RLENGTH); t=substr(t,RSTART+RLENGTH)
        if (!(adr in have_adr))
          printf "%s:%d: changelog-adr-inexistente — \"%s\" não existe em docs/adr/\n", label, NR, adr } }
    }
  ' "$SRC"
}
# Seção 8: restrição apontando um ADR já substituído (Status: Substituído por) → restrição morta.
check_restricao_morta() {
  awk -v label="$LABEL" -v sup="$superseded_ids" '
    BEGIN { ns=split(sup,ss,/[[:space:]]+/); for(i=1;i<=ns;i++) if(ss[i]!="") is_sup[ss[i]]=1 }
    /^## / { sec=$2; sub(/\./,"",sec); next }
    sec=="8" {
      t=$0; while (match(t,/ADR-[0-9]+/)) { adr=substr(t,RSTART,RLENGTH); t=substr(t,RSTART+RLENGTH)
        if (adr in is_sup)
          printf "%s:%d: restricao-morta — a restrição aponta %s, já substituído (veja o Status do ADR)\n", label, NR, adr }
    }
  ' "$SRC"
}

case "$mode" in
  prd)     findings="$(check_stack; check_nfr; check_rf; check_changelog; check_restricao_morta)" ;;
  specify) findings="$(check_stack; check_rf_cobertos)" ;;
esac
```

- [ ] **Step 9: Rodar o teste e verificar que PASSA**

Run: `bash scripts/test-check-prd.sh`
Expected: termina com `test-check-prd: tudo verde` (casos 1–7 ok).

- [ ] **Step 10: Confirmar que a fixture limpa antiga segue limpa (sem regressão)**

Run: `bash scripts/check-prd.sh prd scripts/fixtures/prd-clean.md`
Expected: `check-prd: limpo` (sem §13, sem docs/adr vizinho com ADR-*.md → checks novos silenciosos).

- [ ] **Step 11: Sincronizar references e conferir drift**

Run: `./scripts/sync-assets.sh && ./scripts/check-assets.sh`
Expected: `sync-assets: ok` seguido de `check-assets: sem drift`.

- [ ] **Step 12: Commit**

```bash
git add scripts/check-prd.sh scripts/test-check-prd.sh scripts/fixtures/prd-evolve skills/zion-prd-write/references/check-prd.sh skills/zion-prd-specify-prompt/references/check-prd.sh
git commit -m "feat(check-prd): checks da §13 (changelog) e §8 (restrição morta) do dia 2 (R8)"
```

---

## Task 3: `check-adr.sh` — simetria da supersessão

**Files:**
- Create: `scripts/fixtures/adr/superseded-clean/ADR-002-motor-raster.md`
- Create: `scripts/fixtures/adr/superseded-clean/ADR-005-motor-vetor.md`
- Create: `scripts/fixtures/adr/superseded-dirty/ADR-002-motor-raster.md`
- Create: `scripts/fixtures/adr/superseded-dirty/ADR-005-motor-vetor.md`
- Modify: `scripts/check-adr.sh`
- Test: `scripts/test-check-adr.sh`

- [ ] **Step 1: Criar a fixture simétrica (limpa)**

`scripts/fixtures/adr/superseded-clean/ADR-002-motor-raster.md`:

```markdown
# ADR-002 — Motor de exportação raster

- **Status:** Substituído por ADR-005
- **Data:** 2026-07-17
- **Decisores:** time
- **Evidência:** https://exemplo.org/comparativo-exportacao (licença MIT)

## Contexto
Risco de conhecimento. Decisão documentável sem rodar.

## Decisão
Exportar em raster.

## Consequências
Trade-offs aceitos.

## Status
Substituído por ADR-005.
```

`scripts/fixtures/adr/superseded-clean/ADR-005-motor-vetor.md`:

```markdown
# ADR-005 — Motor de exportação vetorial

- **Status:** Aceito
- **Data:** 2026-08-02
- **Decisores:** time
- **Substitui:** ADR-002
- **Evidência:** https://exemplo.org/exportacao-vetor (licença MIT)

## Contexto
A decisão anterior (ADR-002) caiu: usuários precisam de vetor. Este ADR a substitui.

## Decisão
Exportar em vetor.

## Consequências
Trade-offs aceitos.

## Status
Aceito.
```

- [ ] **Step 2: Criar a fixture assimétrica (suja)**

O ADR-002 se declara substituído por ADR-005, mas o ADR-005 **não** declara `Substitui: ADR-002`
(referência unilateral). Ambos têm Evidência válida — o único achado deve ser a assimetria.

`scripts/fixtures/adr/superseded-dirty/ADR-002-motor-raster.md`:

```markdown
# ADR-002 — Motor de exportação raster

- **Status:** Substituído por ADR-005
- **Data:** 2026-07-17
- **Decisores:** time
- **Evidência:** https://exemplo.org/comparativo-exportacao (licença MIT)

## Contexto
Risco de conhecimento. Decisão documentável sem rodar.

## Decisão
Exportar em raster.

## Consequências
Trade-offs aceitos.

## Status
Substituído por ADR-005.
```

`scripts/fixtures/adr/superseded-dirty/ADR-005-motor-vetor.md` (**sem** a linha `Substitui:`):

```markdown
# ADR-005 — Motor de exportação vetorial

- **Status:** Aceito
- **Data:** 2026-08-02
- **Decisores:** time
- **Evidência:** https://exemplo.org/exportacao-vetor (licença MIT)

## Contexto
Motor de exportação vetorial.

## Decisão
Exportar em vetor.

## Consequências
Trade-offs aceitos.

## Status
Aceito.
```

- [ ] **Step 3: Escrever os asserts falhando em `scripts/test-check-adr.sh`**

Localize o bloco final:

```bash
if [ "$fail" -eq 0 ]; then echo "test-check-adr: tudo verde"; else echo "test-check-adr: FALHOU"; exit 1; fi
```

Insira **antes** dele os casos 4 e 5:

```bash
# 4. Supersessão simétrica → limpa (exit 0)
out="$(bash "$CHECK" "$FIX/superseded-clean")"; rc=$?
assert_exit "supersessão simétrica sai 0" 0 "$rc"
assert_contains "supersessão simétrica reporta limpo" "check-adr: limpo" "$out"

# 5. Supersessão assimétrica (referência unilateral) → exit 1 + achado
out="$(bash "$CHECK" "$FIX/superseded-dirty")"; rc=$?
assert_exit "supersessão assimétrica sai 1" 1 "$rc"
assert_contains "acha supersessao-assimetrica" "supersessao-assimetrica" "$out"

if [ "$fail" -eq 0 ]; then echo "test-check-adr: tudo verde"; else echo "test-check-adr: FALHOU"; exit 1; fi
```

(Apague a linha `if [ "$fail" -eq 0 ]...` antiga — ela agora vem depois dos novos casos.)

- [ ] **Step 4: Rodar o teste e verificar que FALHA**

Run: `bash scripts/test-check-adr.sh`
Expected: FALHA — `FALHOU: supersessão assimétrica sai 1` (a fixture assimétrica ainda sai 0) e o
`assert_contains` de `supersessao-assimetrica` falha.

- [ ] **Step 5: Adicionar os helpers de supersessão ao `scripts/check-adr.sh`**

Localize:

```bash
evidence_value() {  # $1 arquivo
  sed -n 's/^[[:space:]]*-[[:space:]]*\*\*Evidência:\*\*[[:space:]]*//p' "$1" | head -1
}

findings=""
```

Substitua por (insere os três helpers entre a função e `findings=""`):

```bash
evidence_value() {  # $1 arquivo
  sed -n 's/^[[:space:]]*-[[:space:]]*\*\*Evidência:\*\*[[:space:]]*//p' "$1" | head -1
}

# --- R8: supersessão (referência simétrica Substitui / Status: Substituído por) ---
adr_file_for() {  # $1 id (ADR-002) -> caminho do arquivo em $DIR, ou vazio
  local m; m="$(ls "$DIR/$1-"*.md 2>/dev/null | head -1)"; [ -n "$m" ] && printf '%s' "$m"
}
field_substitui() {  # $1 arquivo -> ADR-<n> que declara substituir (maiúsculo), ou vazio
  sed -n 's/^[[:space:]]*-[[:space:]]*\*\*Substitui:\*\*[[:space:]]*//p' "$1" \
    | grep -oiE 'ADR-[0-9]+' | head -1 | tr '[:lower:]' '[:upper:]'
}
status_superseded_by() {  # $1 arquivo -> ADR-<m> pelo qual foi substituído, ou vazio
  local st; st="$(sed -n 's/^[[:space:]]*-[[:space:]]*\*\*Status:\*\*[[:space:]]*//p' "$1" | head -1)"
  printf '%s' "$st" | grep -qiE 'Substitu[ií]do por' || return 0
  printf '%s' "$st" | grep -oiE 'ADR-[0-9]+' | head -1 | tr '[:lower:]' '[:upper:]'
}

findings=""
```

- [ ] **Step 6: Adicionar o loop de simetria antes do relatório final**

Localize o fim do loop de evidência seguido do check de `nadr`:

```bash
    *)
      # Risco de conhecimento: precisa de URL (http…) ou de um caminho de artefato.
      if ! printf '%s' "$ev" | grep -qE 'https?://|/|\.[A-Za-z0-9]+'; then
        add "$label: evidencia-sem-lastro — \"$ev\" sem URL nem caminho de artefato (aponte a fonte)"
      fi
      ;;
  esac
done

if [ "$nadr" -eq 0 ]; then
```

Substitua por (insere o loop de simetria entre o `done` e o `if`):

```bash
    *)
      # Risco de conhecimento: precisa de URL (http…) ou de um caminho de artefato.
      if ! printf '%s' "$ev" | grep -qE 'https?://|/|\.[A-Za-z0-9]+'; then
        add "$label: evidencia-sem-lastro — \"$ev\" sem URL nem caminho de artefato (aponte a fonte)"
      fi
      ;;
  esac
done

# Simetria da supersessão: cada elo Substitui / Status-substituído-por exige o par recíproco.
for f in "$DIR"/ADR-*.md; do
  [ -f "$f" ] || continue
  id="$(basename "$f" | grep -oE '^ADR-[0-9]+')"
  label="$(basename "$f")"
  sub="$(field_substitui "$f")"
  if [ -n "$sub" ]; then
    tf="$(adr_file_for "$sub")"
    if [ -z "$tf" ]; then
      add "$label: supersessao-assimetrica — declara Substitui: $sub, mas $sub não existe em $DIR"
    else
      back="$(status_superseded_by "$tf")"
      [ "$back" = "$id" ] || add "$label: supersessao-assimetrica — Substitui: $sub, mas $sub não tem Status \"Substituído por $id\" (unilateral)"
    fi
  fi
  supby="$(status_superseded_by "$f")"
  if [ -n "$supby" ]; then
    tf="$(adr_file_for "$supby")"
    if [ -z "$tf" ]; then
      add "$label: supersessao-assimetrica — Status aponta $supby, mas $supby não existe em $DIR"
    else
      fwd="$(field_substitui "$tf")"
      [ "$fwd" = "$id" ] || add "$label: supersessao-assimetrica — Status: Substituído por $supby, mas $supby não declara Substitui: $id (unilateral)"
    fi
  fi
done

if [ "$nadr" -eq 0 ]; then
```

- [ ] **Step 7: Rodar o teste e verificar que PASSA**

Run: `bash scripts/test-check-adr.sh`
Expected: termina com `test-check-adr: tudo verde` (casos 1–5 ok; as fixtures `clean`/`dirty` antigas
não têm campos de supersessão → o loop novo fica silencioso nelas).

- [ ] **Step 8: Sincronizar references e conferir drift**

Run: `./scripts/sync-assets.sh && ./scripts/check-assets.sh`
Expected: `sync-assets: ok` seguido de `check-assets: sem drift`.

- [ ] **Step 9: Commit**

```bash
git add scripts/check-adr.sh scripts/test-check-adr.sh scripts/fixtures/adr/superseded-clean scripts/fixtures/adr/superseded-dirty skills/zion-prd-spike/references/check-adr.sh
git commit -m "feat(check-adr): verifica a simetria da supersessão de ADR (R8)"
```

---

## Task 4: Skill nova `zion-prd-evolve`

**Files:**
- Create: `skills/zion-prd-evolve/SKILL.md`

- [ ] **Step 1: Criar `skills/zion-prd-evolve/SKILL.md`**

```markdown
---
name: zion-prd-evolve
description: Ponto de entrada único do dia 2 do harness Zion Build PRD — endereça mudanças pós-release (RF novo, RF alterado/removido, decisão revertida) versionando a PRD (§13 changelog), roteando supersessão de ADR e re-decomposição parcial, e parando nas pontes. Use quando um requisito muda depois da release 1, ou o usuário disser "mudou o escopo", "reverter uma decisão" ou "evoluir a PRD".
argument-hint: "A mudança pós-release em linguagem natural (ex.: \"exportar PNG saiu; entrou exportar SVG\")"
metadata:
  author: zion-build-prd
user-invocable: true
disable-model-invocation: false
---

# zion-prd-evolve — o dia 2 do harness (mudança pós-release)

Ponto de entrada único para mudar um artefato **depois** da release 1: um RF novo, um RF alterado ou
removido, ou uma decisão estruturante revertida. Versiona a PRD (changelog na §13), roteia para os
comandos donos de cada artefato **parando em cada gate**, e termina nas pontes — o ciclo `/speckit.*`
é do usuário. Sequência e fronteira em `references/process-context.md`; regras do dia 2 em
`references/quality-rules.md` `#dia-2`. Contrato de 5 fases; gates aconselham, nunca bloqueiam.

## Cenários canônicos (uma mudança pode combinar mais de um)
- **C1 — RF novo:** requisito que não existia. Toca §6 (RF no épico certo ou épico novo) + §13 +
  re-decomposição parcial do épico + tabela (§12 via trace).
- **C2 — RF alterado ou removido:** requisito muda de significado ou sai de escopo. Toca §6 + §13 +
  fatias do épico afetado + tabela; fatia já com spec → contexto de re-specify montado pela ponte.
- **C3 — Decisão revertida:** decisão estruturante caiu. Toca ADR novo que substitui o antigo
  (referência cruzada simétrica) + §8 (restrições) + §13 + aviso de revisar a `constitution`.

## Fase 0 — Pré-requisito (aconselha)
`docs/PRD.md` deve existir — o dia 2 pressupõe o dia 1. Faltando → avise ("recomendo `/zion-prd-write`
antes") e pare graciosamente.

## Fase 1 — Validar entrada e classificar
O argumento é a descrição da mudança em linguagem natural. Classifique-a em **C1/C2/C3** (pode ser mais
de um). Descrição vaga demais para classificar → **pergunte** em vez de adivinhar (ex.: "essa mudança
cria um requisito novo, altera um existente, ou derruba uma decisão de arquitetura?"). Cuidado com a
decisão que cai **disfarçada de requisito** (parece C2, é C3): se a mudança só se resolve **trocando uma
decisão estruturante já registrada num ADR**, é C3. **Confirme a classificação com o usuário antes de
tocar qualquer arquivo.**

## Fase 2/3 — Plano de toque + execução roteada
Monte e **mostre o plano de toque**: a lista ordenada dos artefatos afetados e o comando dono de cada um.
Execute inline só o que é **barato e local**; para o resto, **pare e delegue com handoff explícito, um
gate por vez** — não encadeie tudo num turno só.

**Inline (você mesmo):**
- **§13 (changelog)** — acrescente uma linha na tabela da §13 da PRD, no formato de `#dia-2`: `Data`,
  `Cenário` (só C1/C2/C3), `Mudança`, `Motivo`, `Artefatos afetados`. O evolve é o dono da escrita do
  changelog.
- **Edição pontual da §6/§8** — o RF novo/alterado na §6 (no épico certo) ou a restrição na §8.

**Delegado (pare em cada gate):**
- **Supersessão de decisão (C3)** → `/zion-adr-new "<título>" --substitui ADR-<n>`: cria o ADR novo com
  `Substitui: ADR-<n>` e marca o antigo `Status: Substituído por ADR-<m>` (referência simétrica).
- **Re-fatiamento do épico afetado (C1/C2)** → `/zion-prd-decompose --epico E<k>`: re-fatia **apenas** o
  épico indicado; fatias já implementadas são intocáveis.
- **Reconciliação da tabela (§12)** → `/zion-prd-trace`: dono único da tabela.
- **Fatia já especificada (C2)** → `/zion-prd-specify-prompt` em enquadramento **re-specify**: revê a
  spec existente contra a mudança, com a linha da §13 como contexto.
- **ADR substituído alimentava a `constitution` (C3)** → aconselhe rodar `/zion-prd-constitution-prompt`
  de novo (não edita a `constitution`).

## Fase 4 — Validar saída (aconselha)
Rode os verificadores do `references/` e ecoe o veredito por item — não reverta:

    bash references/check-prd.sh prd docs/PRD.md
    bash references/check-adr.sh docs/adr

`check-prd.sh` confere a §13 (todo `RF-xx`/`ADR-xxx` citado existe, ou a linha o declara "removido";
Cenário só C1/C2/C3) e a §8 (restrição apontando ADR substituído → `restricao-morta`). `check-adr.sh`
confere a **simetria** da supersessão. Scripts ausentes (instalação parcial) → degrade para conferência
em prosa contra `#dia-2`, com aviso — como as demais skills.

## Saída
A PRD versionada (§13), os artefatos afetados atualizados pelos comandos donos e — quando a mudança toca
uma fatia já especificada — o prompt de re-specify pronto para o usuário disparar. **PARE nas pontes:** o
ciclo `/speckit.*` é do usuário.
```

- [ ] **Step 2: Confirmar que o frontmatter está bem-formado**

Run: `head -9 skills/zion-prd-evolve/SKILL.md`
Expected: as 9 primeiras linhas mostram o bloco `---`/`name:`/…/`---` idêntico ao das outras skills
(`name: zion-prd-evolve`, `user-invocable: true`).

- [ ] **Step 3: Commit**

```bash
git add skills/zion-prd-evolve/SKILL.md
git commit -m "feat(evolve): skill zion-prd-evolve — ponto de entrada do dia 2 (R8)"
```

---

## Task 5: Distribuição — registrar `zion-prd-evolve` no `asset-map.sh`

**Files:**
- Modify: `scripts/asset-map.sh`

- [ ] **Step 1: Adicionar `zion-prd-evolve` às entradas de reference que ele consome**

Localize o array `ASSET_MAP`:

```bash
ASSET_MAP=(
  "assets/quality-rules.md                zion-prd-discovery zion-prd-spike zion-prd-write zion-prd-decompose zion-prd-constitution-prompt zion-prd-specify-prompt zion-prd-plan-prompt"
  "assets/templates/prd-skeleton.md       zion-prd-write"
  "assets/templates/traceability-table.md zion-prd-decompose"
  "assets/process-context.md              zion-prd-discovery zion-prd-spike zion-prd-write zion-prd-decompose zion-adr-new"
  "scripts/check-prd.sh                   zion-prd-write zion-prd-specify-prompt"
  "scripts/trace-prd.sh                   zion-prd-trace zion-prd-decompose"
  "scripts/check-adr.sh                   zion-prd-spike"
)
```

Substitua por (acrescenta `zion-prd-evolve` em quality-rules, process-context, check-prd e check-adr):

```bash
ASSET_MAP=(
  "assets/quality-rules.md                zion-prd-discovery zion-prd-spike zion-prd-write zion-prd-decompose zion-prd-constitution-prompt zion-prd-specify-prompt zion-prd-plan-prompt zion-prd-evolve"
  "assets/templates/prd-skeleton.md       zion-prd-write"
  "assets/templates/traceability-table.md zion-prd-decompose"
  "assets/process-context.md              zion-prd-discovery zion-prd-spike zion-prd-write zion-prd-decompose zion-adr-new zion-prd-evolve"
  "scripts/check-prd.sh                   zion-prd-write zion-prd-specify-prompt zion-prd-evolve"
  "scripts/trace-prd.sh                   zion-prd-trace zion-prd-decompose"
  "scripts/check-adr.sh                   zion-prd-spike zion-prd-evolve"
)
```

- [ ] **Step 2: Sincronizar — cria `skills/zion-prd-evolve/references/`**

Run: `./scripts/sync-assets.sh`
Expected: `sync-assets: ok`

- [ ] **Step 3: Verificar as references geradas do evolve**

Run: `ls skills/zion-prd-evolve/references`
Expected: `check-adr.sh  check-prd.sh  process-context.md  quality-rules.md`

- [ ] **Step 4: Conferir drift**

Run: `./scripts/check-assets.sh`
Expected: `check-assets: sem drift`

- [ ] **Step 5: Commit**

```bash
git add scripts/asset-map.sh skills/zion-prd-evolve/references
git commit -m "build(assets): registra zion-prd-evolve no asset-map e sincroniza references (R8)"
```

---

## Task 6: Modo substituir no `zion-adr-new`

**Files:**
- Modify: `skills/zion-adr-new/SKILL.md`

- [ ] **Step 1: Atualizar o `argument-hint` para citar o `--substitui`**

Localize:

```
argument-hint: "Título da decisão estruturante (ex.: \"Escolha de biblioteca de estado\")"
```

Substitua por:

```
argument-hint: "Título da decisão (ex.: \"Escolha de estado\"); opcional --substitui ADR-<n> no dia 2"
```

- [ ] **Step 2: Documentar o argumento opcional na seção "## Argumento"**

Localize:

```markdown
```text
/zion-adr-new  "Escolha de biblioteca de renderização de diagramas"
/zion-adr-new  "Estratégia de gerenciamento de estado do editor"
```
```

Substitua por (acrescenta o exemplo do dia 2):

```markdown
```text
/zion-adr-new  "Escolha de biblioteca de renderização de diagramas"
/zion-adr-new  "Estratégia de gerenciamento de estado do editor"
/zion-adr-new  "Motor de exportação vetorial" --substitui ADR-002
```

O sufixo opcional `--substitui ADR-<n>` ativa o **modo substituir** (dia 2 — ver abaixo).
```

- [ ] **Step 3: Adicionar o campo `Substitui` ao template gerado**

Localize, no bloco do template:

```markdown
- **Status:** Proposto
- **Data:** <preencher>
- **Decisores:** <preencher>
- **Evidência:** <um dos dois — o tipo casa com o risco da decisão>
```

Substitua por (acrescenta a linha condicional do `Substitui`):

```markdown
- **Status:** Proposto
- **Data:** <preencher>
- **Decisores:** <preencher>
- **Substitui:** <só no modo substituir: ADR-<n>; no modo normal, omita esta linha>
- **Evidência:** <um dos dois — o tipo casa com o risco da decisão>
```

- [ ] **Step 4: Adicionar a seção "## Modo substituir (supersessão) — dia 2"**

Localize a seção "## Convenção do spike dir (risco de execução)" (seu cabeçalho) e insira **antes** dela
a nova seção:

```markdown
## Modo substituir (supersessão) — dia 2

Disparado por `/zion-adr-new "<título>" --substitui ADR-<n>` (ou pelo `/zion-prd-evolve` no C3). Além do
modo normal:

1. **No ADR novo (ADR-<m>):** mantenha a linha de cabeçalho `- **Substitui:** ADR-<n>` e escreva o
   **Contexto** explicando por que a decisão anterior caiu.
2. **No ADR antigo (ADR-<n>):** edite o cabeçalho para `- **Status:** Substituído por ADR-<m>` — a
   referência é **cruzada e simétrica** (cada um aponta o outro). O `check-adr.sh` verifica essa simetria
   (achado `supersessao-assimetrica` quando a referência é quebrada ou unilateral).
3. **Restrição na PRD §8:** a restrição correspondente precisa de atualização — quem edita a §8 é o
   `/zion-prd-evolve`, no plano de toque. Aqui, apenas **lembre** (advisório).

## Convenção do spike dir (risco de execução)
```

Note: o trecho acima termina reabrindo o cabeçalho original "## Convenção do spike dir…", que já existe —
o `old_string` do Edit deve incluir esse cabeçalho uma única vez.

- [ ] **Step 5: Confirmar a estrutura editada**

Run: `grep -nE '^## |Substitui|--substitui' skills/zion-adr-new/SKILL.md`
Expected: aparecem a seção "## Modo substituir (supersessão) — dia 2", a linha `- **Substitui:**` do
template e as menções ao `--substitui`.

- [ ] **Step 6: Commit**

```bash
git add skills/zion-adr-new/SKILL.md
git commit -m "feat(adr-new): modo substituir (supersessão simétrica) para o dia 2 (R8)"
```

---

## Task 7: Modo parcial no `zion-prd-decompose`

**Files:**
- Modify: `skills/zion-prd-decompose/SKILL.md`

- [ ] **Step 1: Atualizar o `argument-hint`**

Localize:

```
argument-hint: "(sem argumento — trabalha sobre docs/PRD.md)"
```

Substitua por:

```
argument-hint: "(sem argumento = modo integral; --epico E<k> = re-fatiar só um épico no dia 2)"
```

- [ ] **Step 2: Anexar a frase de idempotência à Fase 2/3**

Localize (fim do parágrafo da Fase 2/3):

```markdown
Invoque `superpowers:brainstorming` no mesmo turno para: (1) agrupar os `RF-xx` em épicos;
(2) montar o story map (backbone da jornada); (3) cortar linhas de release R0..Rn; (4) fatiar cada
épico em fatias verticais.
```

Substitua por (acrescenta o parágrafo do modo parcial):

```markdown
Invoque `superpowers:brainstorming` no mesmo turno para: (1) agrupar os `RF-xx` em épicos;
(2) montar o story map (backbone da jornada); (3) cortar linhas de release R0..Rn; (4) fatiar cada
épico em fatias verticais.

**Modo parcial (dia 2) — `--epico E<k>`:** re-fatia **apenas** o épico indicado (invocado à mão ou pelo
`/zion-prd-evolve` com o épico afetado). Invoque `superpowers:brainstorming` com escopo naquele épico,
aplicando INVEST/SPIDR como no modo integral. **Fatias já implementadas do épico são intocáveis** — viram
**restrição** do re-fatiamento (as novas fatias partem do que já existe). Não mexa na §12 à mão: ao final,
mande rodar `/zion-prd-trace` (dono único da tabela), que reconcilia sem duplicar. O **modo integral**
continua o default; se a PRD **já** tem backlog decomposto, prefira o modo parcial (`--epico E<k>`).
```

- [ ] **Step 3: Confirmar a estrutura editada**

Run: `grep -nE 'Modo parcial|--epico|intocáveis' skills/zion-prd-decompose/SKILL.md`
Expected: aparecem a menção ao "Modo parcial (dia 2)", ao `--epico E<k>` e às fatias "intocáveis".

- [ ] **Step 4: Commit**

```bash
git add skills/zion-prd-decompose/SKILL.md
git commit -m "feat(decompose): modo parcial (--epico) para re-fatiamento no dia 2 (R8)"
```

---

## Task 8: Enquadramento re-specify no `zion-prd-specify-prompt`

**Files:**
- Modify: `skills/zion-prd-specify-prompt/SKILL.md`

- [ ] **Step 1: Adicionar o enquadramento re-specify à Fase 2/3**

Localize o fim da lista da "## Fase 2/3 — Montar o prompt (você mesmo)":

```markdown
- Pedir explicitamente que o `spec.md` inclua uma linha rotulada **`**RF cobertos:** RF-xx, ...`** com
  os RF que esta fatia cobre — é o elo legível por máquina que o `/zion-prd-trace` grepa para
  reconciliar a rastreabilidade. Declarar *quais* RF a fatia cobre é o-quê/rastreabilidade, não stack:
  não fere a fronteira sem-stack.
```

Substitua por (acrescenta o parágrafo do modo re-specify após a lista):

```markdown
- Pedir explicitamente que o `spec.md` inclua uma linha rotulada **`**RF cobertos:** RF-xx, ...`** com
  os RF que esta fatia cobre — é o elo legível por máquina que o `/zion-prd-trace` grepa para
  reconciliar a rastreabilidade. Declarar *quais* RF a fatia cobre é o-quê/rastreabilidade, não stack:
  não fere a fronteira sem-stack.

**Modo re-specify (dia 2):** quando a fatia apontada **já** tem `specs/<n>-*/spec.md` (invocado pelo
`/zion-prd-evolve` no C2), monte o prompt como **revisão** — "revise a spec existente contra a mudança X"
—, trazendo a linha da §13 (changelog) como contexto, em vez de "especifique do zero". A fronteira
sem-stack fica blindada igual (a stack segue no `plan`), e o `check-prd.sh specify -` verifica igual.
```

- [ ] **Step 2: Confirmar a estrutura editada**

Run: `grep -nE 'Modo re-specify|revise a spec' skills/zion-prd-specify-prompt/SKILL.md`
Expected: aparece o parágrafo "Modo re-specify (dia 2)".

- [ ] **Step 3: Commit**

```bash
git add skills/zion-prd-specify-prompt/SKILL.md
git commit -m "feat(specify-prompt): enquadramento re-specify para o dia 2 (R8)"
```

---

## Task 9: Fixtures LLM do evolve + índice de avaliação

**Files:**
- Create: `scripts/fixtures/skills/evolve/parece-c2-mas-c3/mudanca.md`
- Create: `scripts/fixtures/skills/evolve/parece-c2-mas-c3/esperado.md`
- Create: `scripts/fixtures/skills/evolve/limpa/mudanca.md`
- Create: `scripts/fixtures/skills/evolve/limpa/esperado.md`
- Modify: `docs/avaliacao-harness.md`

- [ ] **Step 1: Criar a entrada disfarçada (parece C2, é C3)**

`scripts/fixtures/skills/evolve/parece-c2-mas-c3/mudanca.md`:

```markdown
# Mudança pós-release — Painel de Tarefas

Os usuários pediram que a exportação do quadro passe a sair em **vetor** (hoje sai em imagem
rasterizada). Isso muda o RF-06 (exportar imagem). O motor de exportação atual, escolhido na ADR-002,
só produz raster — para entregar vetor teríamos que trocar o motor.
```

- [ ] **Step 2: Criar o sidecar `esperado.md` (classifica C3)**

`scripts/fixtures/skills/evolve/parece-c2-mas-c3/esperado.md`:

```markdown
---
skill: zion-prd-evolve
fase: 1
regra: "#dia-2"
defeito: decisao-disfarcada-de-requisito
veredito: C3
achado_esperado:
  - classifica a mudança como C3 (decisão revertida), não apenas C2 (RF alterado)
  - identifica que o driver é trocar o motor de exportação fixado na ADR-002 (uma decisão estruturante)
  - roteia para a supersessão de ADR (`/zion-adr-new --substitui ADR-002`), além de editar o RF-06 e o changelog
---
## Defeito plantado
A mudança **parece** um simples RF alterado (C2 — "exportar vetor em vez de raster"), mas o que a
sustenta é a queda de uma decisão estruturante: o motor de exportação fixado na ADR-002 não gera vetor.
Endereçar só o RF (C2) deixa a ADR-002 viva e a §8 apontando uma restrição morta. É C3 (decisão
revertida) disfarçada de requisito — a classe que a Fase 1 do evolve precisa enxergar.

## Como reconhecer o acerto
A Fase 1 do evolve classifica a mudança como **C3** (ou C2+C3), não só C2: aponta que trocar o motor
implica substituir a ADR-002 por um ADR novo (referência simétrica) e atualizar a restrição da §8, além
de editar o RF-06 e registrar a linha no changelog. Um falso-negativo é classificar como só C2 e seguir
para re-specify sem tocar o ADR — deixando a decisão revertida sem lastro.
```

- [ ] **Step 3: Criar a entrada limpa (guarda de falso-positivo)**

`scripts/fixtures/skills/evolve/limpa/mudanca.md`:

```markdown
# Mudança pós-release — Painel de Tarefas

Além de apontar o erro de sintaxe, o editor agora também deve **sugerir a correção** provável. É uma
ampliação do RF-02 (apontar erro de sintaxe). Nenhuma decisão de arquitetura muda: o mesmo motor e a
mesma persistência seguem valendo.
```

- [ ] **Step 4: Criar o sidecar `esperado.md` (classifica C2)**

`scripts/fixtures/skills/evolve/limpa/esperado.md`:

```markdown
---
skill: zion-prd-evolve
fase: 1
regra: "#dia-2"
defeito:
veredito: C2
achado_esperado:
  - classifica a mudança como C2 (RF alterado), sem inventar um C3
  - roteia para editar o RF-02 na §6, registrar o changelog na §13 e re-fatiar/re-especificar o épico afetado
  - não dispara supersessão de ADR (nenhuma decisão estruturante caiu)
---
## Defeito plantado
Nenhum — é uma alteração de RF genuína (C2), sem decisão estruturante por baixo. A própria mudança diz
que a arquitetura não muda. Serve de guarda contra o falso-positivo de classificar tudo como C3.

## Como reconhecer o acerto
A Fase 1 do evolve classifica como **C2** e roteia para editar o RF-02, registrar o changelog e
re-fatiar/re-especificar o épico afetado — **sem** abrir supersessão de ADR. Um falso-positivo é enxergar
um C3 onde não há decisão revertida e mandar criar um ADR à toa.
```

- [ ] **Step 5: Indexar as fixtures novas em `docs/avaliacao-harness.md`**

Localize a última linha da tabela "### LLM (camada de julgamento — sob demanda)":

```markdown
| decompose | `limpa` | `backlog.md` | — (backlog vertical + skeleton em R0) | aprova |
```

Substitua por (acrescenta as duas linhas do evolve):

```markdown
| decompose | `limpa` | `backlog.md` | — (backlog vertical + skeleton em R0) | aprova |
| evolve | `parece-c2-mas-c3` | `mudanca.md` | decisão revertida disfarçada de RF alterado | classifica C3 |
| evolve | `limpa` | `mudanca.md` | — (RF alterado genuíno) | classifica C2 |
```

- [ ] **Step 6: Anotar a semântica do veredito de classificação**

Localize, logo abaixo da tabela LLM:

```markdown
Cada skill tem ao menos um par **defeito/`limpa`** — a suíte pega falso-negativo (não acusou o defeito)
e falso-positivo (reprovou o que estava bom).
```

Substitua por:

```markdown
Cada skill tem ao menos um par **defeito/`limpa`** — a suíte pega falso-negativo (não acusou o defeito)
e falso-positivo (reprovou o que estava bom).

> As fixtures do `evolve` testam a **classificação da Fase 1** (não um veredito aprova/reprova de um
> artefato): o campo `veredito` do `esperado.md` carrega a **classe esperada** (C1/C2/C3). O acerto é
> classificar na classe certa — o par `parece-c2-mas-c3`/`limpa` pega tanto o falso-negativo (não viu o
> C3 disfarçado) quanto o falso-positivo (inventou um C3 onde só havia C2).
```

- [ ] **Step 7: Confirmar o frontmatter das fixtures**

Run: `grep -rn 'veredito:' scripts/fixtures/skills/evolve/`
Expected: `parece-c2-mas-c3/esperado.md:` mostra `veredito: C3` e `limpa/esperado.md:` mostra `veredito: C2`.

- [ ] **Step 8: Commit**

```bash
git add scripts/fixtures/skills/evolve docs/avaliacao-harness.md
git commit -m "test(eval): fixtures LLM de classificação do evolve + índice (R8)"
```

---

## Task 10: Documentação — guia (Passo 7), como-usar, README

**Files:**
- Modify: `docs/guia-prd-para-spec-kit.md`
- Modify: `docs/como-usar.md`
- Modify: `README.md`

- [ ] **Step 1: Adicionar o "Passo 7 — O dia 2" ao guia**

Em `docs/guia-prd-para-spec-kit.md`, localize:

```markdown
## Implementação das skills

Para cada skill usada no processo: **gatilho** (como invocar) e **papel** no passo.
```

Substitua por (insere o Passo 7 + separador antes de "## Implementação das skills"):

```markdown
## Passo 7 — O dia 2: evoluir os artefatos (`/zion-prd-evolve`)

- **Objetivo:** endereçar mudanças que chegam **depois** da release 1 sem que os artefatos-guia
  congelem. A tese do método é que o custo da PRD, dos ADRs e da rastreabilidade só se paga se eles
  **viverem mais que a release 1**: artefato-guia que não evolui vira documentação morta. O dia 2 tem um
  **dono com gatilho** — um comando —, não prosa solta num guia.
- **Skill:** `zion-prd-evolve` (real) — ponto de entrada único do dia 2.
- **Os três cenários canônicos:**
  - **C1 — RF novo:** um requisito que não existia. Entra na §6 (no épico certo ou num épico novo), vira
    linha no changelog (§13), dispara re-decomposição **parcial** do épico e reconciliação da tabela.
  - **C2 — RF alterado ou removido:** um requisito muda de significado ou sai de escopo. Toca a §6, o
    changelog e as fatias do épico afetado; se a fatia já tem `spec.md`, a ponte monta um prompt de
    **re-specify** (revê a spec contra a mudança) em vez de especificar do zero.
  - **C3 — Decisão revertida:** uma decisão estruturante caiu. Nasce um ADR novo que **substitui** o
    antigo (referência cruzada simétrica: `Substitui:` / `Status: Substituído por`), a restrição da §8 é
    atualizada, e o evolve aconselha rodar a `constitution` de novo se o ADR alimentava um princípio.
- **O fluxo pelo evolve:** ele **classifica** a mudança (pode ser mais de um cenário — cuidado com a
  decisão que cai disfarçada de requisito, que parece C2 e é C3), **confirma com você**, mostra o
  **plano de toque** (que artefato muda e qual comando é dono), executa inline só o barato e local (o
  changelog e a edição pontual da §6/§8) e **para em cada gate** delegando o resto:
  `/zion-adr-new --substitui` (supersessão), `/zion-prd-decompose --epico E<k>` (re-fatiamento parcial),
  `/zion-prd-trace` (tabela), `/zion-prd-specify-prompt` (re-specify). **Termina na ponte:** o ciclo
  `/speckit.*` é seu.
- **Verificação:** a Fase 4 roda `check-prd.sh` (confere a §13 e a §8) e `check-adr.sh` (confere a
  simetria da supersessão) — os mesmos checks que beneficiam quem edita à mão.
- **Fora do escopo do dia 2:** não orquestra o `/speckit.*`, não edita a `constitution` (só aconselha a
  ponte), não versiona a PRD com número semântico (só o changelog) e não detecta drift por histórico de
  git (auditoria contínua é outro gap).

---

## Implementação das skills

Para cada skill usada no processo: **gatilho** (como invocar) e **papel** no passo.
```

- [ ] **Step 2: Adicionar a linha do evolve à tabela de implementação do guia**

Localize (na tabela "## Implementação das skills"):

```markdown
| `/zion-prd-trace` | Skill tool ou o comando homônimo | **Rastreabilidade mecânica (P6)** — reconcilia a seção 12 da PRD a partir das `specs/*/spec.md` (RF↔spec + status ☐/◐/● do `tasks.md`); a tabela é derivada, não mantida à mão. |
```

Substitua por (acrescenta a linha do evolve logo após):

```markdown
| `/zion-prd-trace` | Skill tool ou o comando homônimo | **Rastreabilidade mecânica (P6)** — reconcilia a seção 12 da PRD a partir das `specs/*/spec.md` (RF↔spec + status ☐/◐/● do `tasks.md`); a tabela é derivada, não mantida à mão. |
| `/zion-prd-evolve` | Skill tool ou o comando homônimo | **Dia 2 (P7)** — classifica a mudança pós-release (C1/C2/C3), versiona a PRD (§13), executa inline o barato e local e roteia o resto (adr-new `--substitui`, decompose `--epico`, trace, specify re-specify) parando em cada gate. |
```

- [ ] **Step 3: Corrigir a contagem de skills no `docs/como-usar.md` (linha do intro)**

Localize:

```markdown
> **O que este documento é:** um **guia prático** dos 7 comandos `/prd-*` — o harness que
```

Substitua por (mata o drift H7 "7 comandos" sem re-fixar número que envelhece):

```markdown
> **O que este documento é:** um **guia prático** dos comandos `/prd-*` — o harness que
```

- [ ] **Step 4: Corrigir "9 skills" → "10 skills"**

Localize:

```markdown
Isso instala as 9 skills em `.claude/skills/` do seu projeto.
```

Substitua por:

```markdown
Isso instala as 10 skills em `.claude/skills/` do seu projeto.
```

- [ ] **Step 5: Adicionar a linha do evolve ao "Mapa rápido dos comandos"**

Localize:

```markdown
| `/zion-prd-trace` | 6 · Rastreabilidade | `docs/PRD.md` (§6+§12) + `specs/` | seção 12 reconciliada | `scripts/trace-prd.sh` |
```

Substitua por (acrescenta a linha do evolve):

```markdown
| `/zion-prd-trace` | 6 · Rastreabilidade | `docs/PRD.md` (§6+§12) + `specs/` | seção 12 reconciliada | `scripts/trace-prd.sh` |
| `/zion-prd-evolve` | 7 · Dia 2 (pós-release) | `docs/PRD.md` + a mudança | §13 + edições roteadas | `/zion-adr-new`, `/zion-prd-decompose`, `/zion-prd-trace`, `/zion-prd-specify-prompt` |
```

- [ ] **Step 6: Adicionar o nó do evolve ao diagrama mermaid**

Localize:

```
    G -.->|handoff 1×| H["/speckit.constitution (você)"]
    E -.->|handoff| F["/speckit.* (você)"]
    P -.->|handoff| F
```

Substitua por (acrescenta o nó de dia 2 e suas arestas tracejadas antes do fim do bloco):

```
    G -.->|handoff 1×| H["/speckit.constitution (você)"]
    E -.->|handoff| F["/speckit.* (você)"]
    P -.->|handoff| F
    C -.->|dia 2| V["/zion-prd-evolve"]
    V -.->|C1/C2 parcial| D
    V -.->|C2 re-specify| E
    V -.->|reconcilia| T
```

- [ ] **Step 7: Adicionar a seção de uso "### Dia 2 — `/zion-prd-evolve`"**

Localize (fim da ponte plan-prompt, antes do separador que fecha "Caminho feliz"):

```markdown
/speckit.plan "Realize o spec.md desta feature (prévia ao digitar + persistência entre sessões)
honrando estas decisões já fechadas, sem reabri-las: ADR-001 (motor de render escolhido), ADR-003
(persistência local escolhida). Descreva a stack, a arquitetura e as restrições técnicas que
decorrem dessas decisões e realizam o resultado observável do spec.md."
```

**PARE.** A partir daqui o ciclo `/speckit.*` é seu.

---
```

Substitua por (acrescenta a seção do dia 2 antes do separador):

````markdown
/speckit.plan "Realize o spec.md desta feature (prévia ao digitar + persistência entre sessões)
honrando estas decisões já fechadas, sem reabri-las: ADR-001 (motor de render escolhido), ADR-003
(persistência local escolhida). Descreva a stack, a arquitetura e as restrições técnicas que
decorrem dessas decisões e realizam o resultado observável do spec.md."
```

**PARE.** A partir daqui o ciclo `/speckit.*` é seu.

### Dia 2 — `/zion-prd-evolve` (quando um requisito muda pós-release)

O harness não termina na release 1. Quando um requisito muda depois — um RF novo, um RF alterado/removido
ou uma decisão que caiu —, `/zion-prd-evolve` é o ponto de entrada único. Exemplo:

```text
/zion-prd-evolve Exportar PNG saiu de escopo; agora exportamos SVG — o motor de render atual não gera vetor.
```

Ele **classifica** (aqui é C2 **e** C3: o RF mudou *e* a decisão do motor caiu), **confirma com você** e
mostra o **plano de toque**:

> Plano de toque (C2+C3):
> 1. §13 changelog — eu escrevo a linha da mudança. *(inline)*
> 2. §6/§8 — eu edito o RF de exportação e a restrição. *(inline)*
> 3. Supersessão do ADR do motor → `/zion-adr-new "Motor de exportação vetorial" --substitui ADR-002`. *(gate)*
> 4. Re-fatiar o épico de exportação → `/zion-prd-decompose --epico E3`. *(gate)*
> 5. Fatia já especificada → `/zion-prd-specify-prompt` em modo re-specify. *(gate)*
> 6. Reconciliar a tabela → `/zion-prd-trace`. *(gate)*
> 7. O ADR alimentava a constitution? Aconselho rodar `/zion-prd-constitution-prompt` de novo. *(aviso)*

Executa inline só o barato e local (§13 + edição pontual) e **para em cada gate**, delegando um por vez.
A Fase 4 roda `check-prd.sh` (confere a §13 e a §8) e `check-adr.sh` (confere a simetria da supersessão).
**Termina na ponte:** o ciclo `/speckit.*` é seu.

---
````

- [ ] **Step 8: Corrigir "12 seções" → "13 seções" (dois pontos em como-usar)**

Localize:

```markdown
`assets/templates/prd-skeleton.md` → `docs/PRD.md` (12 seções em branco). **Fase 3** delega a
```

Substitua por:

```markdown
`assets/templates/prd-skeleton.md` → `docs/PRD.md` (13 seções em branco). **Fase 3** delega a
```

Depois localize:

```markdown
- **Esqueleto da PRD** (12 seções): `assets/templates/prd-skeleton.md`.
```

Substitua por:

```markdown
- **Esqueleto da PRD** (13 seções): `assets/templates/prd-skeleton.md`.
```

- [ ] **Step 9: Adicionar a linha do evolve à tabela de skills do `README.md`**

Localize:

```markdown
| `/zion-adr-new` | Cria um ADR em `docs/adr/` |
```

Substitua por (acrescenta a linha do evolve após a do adr-new):

```markdown
| `/zion-adr-new` | Cria um ADR em `docs/adr/` |
| `/zion-prd-evolve` | Dia 2 — mudança pós-release (RF novo/alterado, decisão revertida) |
```

- [ ] **Step 10: Verificar que os documentos não deixaram contagens antigas**

Run: `grep -nE '7 comandos|9 skills|12 seções' docs/como-usar.md`
Expected: nenhuma linha (todas as contagens antigas foram corrigidas).

- [ ] **Step 11: Commit**

```bash
git add docs/guia-prd-para-spec-kit.md docs/como-usar.md README.md
git commit -m "docs: Passo 7 (dia 2), seção do evolve, contagens 9→10 e drift H7 (R8)"
```

---

## Task 11: Verificação final de ponta a ponta

**Files:**
- (nenhum arquivo novo — só verificação)

- [ ] **Step 1: Rodar a suíte mecânica inteira (o que o CI roda)**

Run: `./scripts/eval.sh`
Expected: `=== eval: prd ===` … `test-check-prd: tudo verde`; `=== eval: adr ===` … `test-check-adr: tudo verde`; `=== eval: trace ===` … `test-trace-prd: ...`; e por fim `eval: tudo verde`.

- [ ] **Step 2: Conferir que não há drift de assets**

Run: `./scripts/check-assets.sh`
Expected: `check-assets: sem drift`

- [ ] **Step 3: Conferir que a skill nova está completa e autocontida**

Run: `ls skills/zion-prd-evolve && ls skills/zion-prd-evolve/references`
Expected: `SKILL.md  references` e, dentro de references, `check-adr.sh  check-prd.sh  process-context.md  quality-rules.md`.

- [ ] **Step 4: Sanidade — check-prd e check-adr contra as fixtures novas, à mão**

Run:
```bash
bash scripts/check-prd.sh prd scripts/fixtures/prd-evolve/clean/PRD.md
bash scripts/check-prd.sh prd scripts/fixtures/prd-evolve/dirty/PRD.md
bash scripts/check-adr.sh scripts/fixtures/adr/superseded-clean
bash scripts/check-adr.sh scripts/fixtures/adr/superseded-dirty
```
Expected, na ordem: `check-prd: limpo`; `check-prd: N achado(s)` (com `changelog-*` e `restricao-morta`); `check-adr: limpo`; `check-adr: 1 achado(s)` (`supersessao-assimetrica`).

- [ ] **Step 5: Conferir o histórico de commits do R8**

Run: `git log --oneline -12`
Expected: os commits do R8, de Task 1 a Task 10, na ordem.

---

## Self-Review

**Cobertura da spec (`2026-07-17-r8-dia-2-evolve-design.md`):**
- Skill nova `/zion-prd-evolve` (5 fases, gates aconselham) → Task 4; references distribuídas → Task 5.
- Changelog §13 no esqueleto + regras decidíveis em `#dia-2` → Task 1.
- Supersessão no `zion-adr-new` (`--substitui`, `Substitui:`, Status simétrico, lembrete §8) → Task 6.
- Re-decomposição parcial no `zion-prd-decompose` (`--epico`, fatias intocáveis, idempotência) → Task 7.
- Enquadramento re-specify no `zion-prd-specify-prompt` → Task 8.
- Checks novos: `check-prd.sh` §13 (RF/ADR existente ou "removido", Cenário C1/C2/C3) + §8 (restrição morta), compatível com PRD sem §13 → Task 2; `check-adr.sh` simetria → Task 3.
- Fixtures mecânicas (changelog com RF inexistente, §8 apontando ADR substituído, par de ADRs assimétrico + as limpas) → Tasks 2 e 3; fixtures LLM (classificação C2-disfarçada-de-C3 + limpa) → Task 9.
- Documentação: guia Passo 7 + tese do artefato que congela; como-usar (seção, mapa, mermaid, contagens 9→10, drift H7); README; `quality-rules.md` `#dia-2` → Tasks 1, 9 e 10.
- Infra de distribuição: entradas no `asset-map.sh` + sync → Task 5.
- Verificação (eval, check-assets, checks à mão) → Task 11.

**Fronteira do escopo (o que R8 NÃO faz):** o plano não orquestra `/speckit.*` (para nas pontes), não edita a `constitution` (só aconselha), não introduz versão semântica (só changelog), não cria auditoria por git — coerente com a §"O que R8 NÃO faz".

**Consistência de tipos/nomes:** os identificadores de achado são estáveis entre script e teste — `changelog-cenario-invalido`, `changelog-rf-inexistente`, `changelog-adr-inexistente`, `restricao-morta` (check-prd) e `supersessao-assimetrica` (check-adr). Os campos de cabeçalho de ADR são `- **Substitui:** ADR-<n>` e `- **Status:** Substituído por ADR-<m>`, idênticos no template do adr-new (Task 6), nas fixtures (Tasks 2/3) e nos scripts (Tasks 2/3). Os modos são `--substitui ADR-<n>` (adr-new) e `--epico E<k>` (decompose), idênticos na skill do evolve, nas skills-alvo e na doc.

---

## Execution Handoff

**Plano completo e salvo em `docs/superpowers/plans/2026-07-17-r8-dia-2-evolve.md`. Duas opções de execução:**

**1. Subagent-Driven (recomendado)** — despacho um subagente novo por task, reviso entre elas, iteração rápida.

**2. Inline Execution** — executo as tasks nesta sessão com `executing-plans`, em lotes com checkpoints.

**Qual abordagem?**
